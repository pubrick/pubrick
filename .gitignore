node_modules/
dist/
.next/
.turbo/
*.tsbuildinfo
.env
.env.local
coverage/
.superpowers/
# Regenerated by `next dev` on every run (next.config's `agentRules`).
# `next-env.d.ts` stays tracked — Next's docs say to commit that one.
apps/web/AGENTS.md
apps/web/CLAUDE.md
